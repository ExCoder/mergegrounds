# Changelog

## 1.0.0 — 2026-09-05

- Launch MergeGrounds as the fresh public identity for the fail-closed multi-stack starter, Codex skill, evidence runner, and mutation-testing policy.

- Replace checkbox-only admission with a fail-closed, per-change structured contract.
- Require a base-resident design record, observable acceptance oracle, negative failure-mode tests, and a design-only bootstrap lane.
- Separate author declarations, executable evidence, advisory model output, and trusted admission decisions.
- Add independent adversarial challenge and human explain-back requirements.
- Add conditional assurance for RAG, long context, production evaluations, fine-tuning, providers, and agent tool execution.
- Bind authoritative GitHub admission and challenge checks to an independently administered verifier app.
- Expand governance toward end-to-end delivery, rework, escaped defects, maintainability, comprehension, and total cost.
- Map all eight supplied transcript themes to falsifiable controls without treating anecdotes or benchmark numbers as policy evidence.
- Harden the judge itself against Python import shadowing, Git environment/replacement-object substitution, candidate-local Actions, mutable OCI images, PR-output laundering, dynamic runner/container/shell selection, hostile evidence links, forged attempt receipts, impossible or incomplete AI metric denominators, cross-baseline fine-tuning replay, and executable-bit drift through a schema-v2 content/mode lock.
- Reject unresolved all-zero AI evidence digests, retain the MergeGrounds license and assurance boundary during bootstrap without replacing the target project's license, and require full-plugin installation because the skill depends on repository assets.
- Add a read-only CodeQL SARIF diagnostic boundary with an exact pinned-version output contract, bounded fail-closed parser, zero-finding policy, and subject manifest; explicitly avoid claiming CLI byte identity and reserve authoritative SAST evidence for an independent protected rerun with trusted-Git scope reconciliation.
- Pin the portable workflow Python runtime to 3.13.15 instead of following the moving 3.13 patch alias.
- Add a protected repo-local marketplace catalog and executable full-plugin installation path.
- Separate the green upstream self-dogfood adapter from deliberately red bootstrap output.
- Run the complete unittest suite in GitHub Actions and add locked Ruff, strict mypy, branch coverage, critical mutation, security, deterministic build, and parser-fuzz stages for the source repository.
- Add deterministic release archives, a digest manifest, checksums, lifecycle documentation, community health files, and structured feature/integration intake.
- Replace checkout-dependent first-install instructions with direct immutable-ref
  Codex CLI installation, explicit Git-ref update, truthful local refresh,
  machine-readable status, and complete uninstall/recovery guidance.
- Bind the release workflow to a GitHub-verified public maintainer commit, an
  annotated version tag, its peeled commit, a clean tree, and protected
  default-branch ancestry; compare two builds and issue exact-subject
  GitHub/Sigstore provenance in a separate no-checkout job without
  auto-publishing a Release.
- Document candidate download, checksum/provenance verification, separate
  immutable-release promotion, and the unsigned annotated `v1.0.0` tag boundary.
- Disclose the single-maintainer founding-release bootstrap and preserve two
  independent non-author human reviewers as a hard gate for future R4 releases
  and maximum-assurance production use.
- Route conduct abuse through GitHub's official reporting flow and describe PVR
  as a launch-time external setting instead of claiming nonexistent private
  project inboxes.

This is a breaking policy release: existing repositories must land the design/change-control bootstrap separately before enforcing the new PR contract.
